import { PublicPostsImpurePipe } from './public-posts-impure.pipe';

describe('PublicPostsImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new PublicPostsImpurePipe();
    expect(pipe).toBeTruthy();
  });
});

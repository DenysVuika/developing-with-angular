import { HostPropertiesDirective } from './host-properties.directive';

describe('HostPropertiesDirective', () => {
  it('should create an instance', () => {
    const directive = new HostPropertiesDirective();
    expect(directive).toBeTruthy();
  });
});

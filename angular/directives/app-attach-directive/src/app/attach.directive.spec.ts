import { AttachDirective } from './attach.directive';

describe('AttachDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachDirective(null, null);
    expect(directive).toBeTruthy();
  });
});

import { ClickmeDirective } from './clickme.directive';

describe('ClickmeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickmeDirective();
    expect(directive).toBeTruthy();
  });
});
